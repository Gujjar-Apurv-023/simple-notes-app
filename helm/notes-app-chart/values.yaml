# Default values for notes-app-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: us-central1-docker.pkg.dev/project-3fb9dc72-feba-49ea-b89/devops-repo/simple-notes-app
  # This sets the pull policy for images.
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: LoadBalancer
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 5000

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: "gce"
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: "*"
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

# -- Expose the service via gateway-api HTTPRoute
# Requires Gateway API resources and suitable controller installed within the cluster
# (see: https://gateway-api.sigs.k8s.io/guides/)
httpRoute:
  # HTTPRoute enabled.
  enabled: false
  # HTTPRoute annotations.
  annotations: {}
  # Which Gateways this Route is attached to.
  parentRefs:
  - name: gateway
    sectionName: http
    # namespace: default
  # Hostnames matching HTTP header.
  hostnames:
  - chart-example.local
  # List of rules and filters applied.
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /headers
  #   filters:
  #   - type: RequestHeaderModifier
  #     requestHeaderModifier:
  #       set:
  #       - name: My-Overwrite-Header
  #         value: this-is-the-only-value
  #       remove:
  #       - User-Agent
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /echo
  #     headers:
  #     - name: version
  #       value: v2
resources:
  requests:
    cpu: 100m        # Minimum CPU guaranteed for the container
    memory: 128Mi    # Minimum memory guaranteed

  limits:
    cpu: 500m        # Maximum CPU the container can use
    memory: 512Mi    # Maximum memory the container can use
# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:  
  httpGet:                 # Kubernetes will perform an HTTP request to check container health
    path: /                # The endpoint path that Kubernetes will call (http://pod-ip:port/)
    port: 5000             # The container port where the application is running
  initialDelaySeconds: 10  # Wait 10 seconds after container starts before performing the first health check
  periodSeconds: 10        # Kubernetes will run the liveness check every 10 seconds
  timeoutSeconds: 5        # If the application does not respond within 5 seconds, the check fails
  failureThreshold: 3      # After 3 consecutive failures, Kubernetes will restart the container
readinessProbe:
  httpGet:                # Kubernetes sends an HTTP request to check if the app is ready to accept traffic
    path: /               # Endpoint used to check readiness
    port: 5000            # Port where the application is listening
  initialDelaySeconds: 5  # Wait 5 seconds before the first readiness check
  periodSeconds: 5        # Run readiness check every 5 seconds
  timeoutSeconds: 3       # If the app does not respond within 3 seconds, the check fails
  failureThreshold: 3     # After 3 failures the pod is marked "Not Ready"

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70


nodeSelector: {}
tolerations: []
affinity: {}
volumes: []
# targetMemoryUtilizationPercentage: 70

# Example Persistent Volume Claim configuration
# Uncomment this section if the application needs persistent storage
# volumes:
#   - name: app-storage
#     persistentVolumeClaim:
#       claimName: notes-pvc

# volumeMounts:
#   - name: app-storage
#     mountPath: /data
#     readOnly: false

# Example nodeSelector configuration
# This forces the pod to run only on nodes with a specific label
# Example: node labeled as "workload=backend"
# nodeSelector:
#   workload: backend


# Example tolerations configuration
# This allows pods to run on nodes that have taints
# Example: node dedicated for backend workloads
# tolerations:
#   - key: "dedicated"
#     operator: "Equal"
#     value: "backend"
#     effect: "NoSchedule"


# Example affinity configuration
# Used for advanced scheduling rules
# Example: run pods only on nodes labeled "workload=backend"
# affinity:
#   nodeAffinity:
#     requiredDuringSchedulingIgnoredDuringExecution:
#       nodeSelectorTerms:
#         - matchExpressions:
#             - key: workload
#               operator: In
#               values:
#                 - backend
